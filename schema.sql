CREATE TABLE Cotacao (
    Code       varchar(255),
	CodeIn     varchar(255),
	NamePrice       varchar(255),
	Hight      varchar(255),
	Low        varchar(255),
	PctChange  varchar(255),
	BID        varchar(255),
	Ask        varchar(255),
	TimestampPrice  varchar(255),
	CreateDate varchar(255)
);